# P3 owns — emits real-time pipeline events
# P1 consumes these events to populate the validator feed and agency panels

def emit(event_type: str, agent: str, output: dict, violation: str = None):
    # TODO: stream event to frontend
    pass
